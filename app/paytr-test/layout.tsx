import React from "react";

// Bu sayfa panel layout'undan bağımsız tam ekran render edilir
export default function PayTRTestLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
